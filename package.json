{
  "name": "moonfall-a-point-and-click-adventure",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A portable, original browser adventure inspired by the golden age of point-and-click games.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check game.js && node --check game-logic.js && node --check voice-lines.js && node --check scripts/generate-voices.mjs && node --check scripts/bump-version.mjs && node --check server.mjs && node --check sw.js && node --check tests/game-logic.test.js",
    "test": "node --test tests/*.test.js",
    "test:ci": "node --test tests/*.test.js 2>&1",
    "generate:voices": "node scripts/generate-voices.mjs",
    "bump-version": "node scripts/bump-version.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "kokoro-js": "^1.2.1"
  }
}
